server:
  port: 8013
  servlet:
    encoding:
      enabled: true
      force: true
      charset: UTF-8

spring:
  application:
    name: SAA-13ToolCalling
  ai:
    dashscope:
      api-key: ${DASHSCOPE_API_KEY}

